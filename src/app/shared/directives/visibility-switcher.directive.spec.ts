import { VisibilitySwitcherDirective } from './visibility-switcher.directive';

describe('VisibilitySwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilitySwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
