import { ChartContainerDirective } from './chart-container.directive';

describe('ChartContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartContainerDirective();
    expect(directive).toBeTruthy();
  });
});
